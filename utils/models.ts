export interface Coordinates {
    latitude: number;
    longitude: number;
}

export interface PickerItemModel {
    label: string;
    value: string;
}

export interface User {
    name: string;
    email: string;
}

